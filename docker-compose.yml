version: "3.9"
services:
  app:
    build: .
    env_file: .env
    depends_on:
      - redis
    volumes:
      - nexus-data:/root/.nexus
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
volumes:
  nexus-data:
